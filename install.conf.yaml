- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.zshenv: 
    ~/.gitconfig: 
    ~/.config/gh:
      path: ~/Dropbox/_dotfiles/config/gh
      ignore-missing: true
    ~/.config/bat:
      create: true
      path: config/bat

- create:
  - ~/workspace
  - ~/workspace/courses
  - ~/workspace/sandbox

- shell:
  - 
    command: ./setup_homebrew.sh
    description: Install Homebrew
    stdin: true
    stdout: true
    stderr: true
