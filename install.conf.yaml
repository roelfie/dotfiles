- defaults:
    link:
      relink: true

- clean: ['~']

# - link:
#     ~/.tmux.conf: tmux.conf
#     ~/.vim: vim
#     ~/.vimrc: vimrc

- create:
  - ~/workspace2

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]