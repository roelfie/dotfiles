- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
  - ~/workspace
  - ~/workspace/courses
  - ~/workspace/sandbox

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]